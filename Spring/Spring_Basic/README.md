# SpringBoot\_Basic스프링 - 스프링부트 , 스프링 프레임워크 ->필수스프링 데이터 - sql , nosql , crud 도와주는것 jpl을많이씀 ->선택---&#x09;		  스프링세션 , 프스링 시큐리티 , 스프링 rest Docs  ,  스프링배치 : 실무에서 몇명(천명,백명씩 나누는것)   등스프링 프레임워크 - 핵심기술 : 스프링 DI컨테이너 , aop웹기술 : 스프링MVC , 스프링webflux&#x09;				데이터 접근기술 : 트랜잭션 , jdbc , orm&#x09;				기술 통합 : 캐시,이메일 ,원격접근테스트&#x09;		언어: 코틀린,그루비스프링부트: 스프링을 편리하게 사용할수 있도록 지원 ,tomcat같은 웹서버 내장해서 웹서버 설치 필요x ,빌드구성을 위한starter종속성## 스프링 - 자바 언어 기반의 프레임워크 , 객체 지향 어플리케이션을 개발할 수 있게 도와주는 프레임워크\##다형성자동차 역할 ,  자동차 구현(k3,아반테) -> 새로운 자동차가 나와도 운전자 역할은 운전할 수 있음역할과 구현으로 분리하면 클라이언트는 대상의역할(인터페이스)만 알면되고 내부 구조를 몰라도되며 변경되어도 영향이없다.하는이유: 클라이언트가 인터페이스만알고 구현 대상의 내부구조변경 , 구현대상 자체변경에 영향을 안받음역할 = 인터페이스  구현 = 인터페이스를 구현한 클래스,객체객체를 설계할 때 역할과 구현을 분리한다. 역할(인터페이스)을 먼저 부여하고 그다음에 구현 객체 만들기오버라이딩이 그 예제 오버라이딩 : 부모클래스를 자식 클래스단에서 수정하는것.객체지향의 SOLID 원칙(좋은객체지향)SRP 단일 책임원칙 : 한클래스는 하나의 책임만 -> 변경이 있을때 파급효과가 적다면 단일책임원칙을 잘따른것(UI변경, 객체의 생성과 사용분리)OCP 개방 폐쇄원칙 :  소프트웨어 요소는 확장에는 열려있으나 변경에는 닫혀있어야한다. 다형성활용 -> 인터페이스를 구현한 새로운 클래스를 만들어서 새로운기능(자동차) 추가    문제점-> 클라리언트가 객체를 변경하려면 클라이언트코드를 변경해야한다. 해결방법-> DI 의존성LSP 리스코프 치환 원칙  : 객체는 프로그램의 정확성을 깨뜨리지않고 하위 타입의 인스턴스로 바꿀수있어야한다.ex) 자동차 인터페이스의 엑셀은 앞으로 가야하는데 뒤로가게 구현하면 위반ISP 인터페이스 분리 원칙 : 자동차 인터페이스는 자동차 인터페이스 하나보다 운전, 정비로 나누는게 낫다. 특정 클라이언트를 위한 인터페이스 여러개가 범용 인터페이스 하나보다 낫다.DIP 의존관계 역전 원칙: 구현 클래스에 의존하지않고 인터페이스에 의존 , 클라이언트가 인터페이스에 의존하게. 구현체에 의존하면 변경이 어렵.요구사항과 설계회원 - 1.회원가입조회 2. 회원은 일반과 vip 3. 회원데이터는 자체 db구축가능 외부시스템연동가능주문과 할인 - 회원은 상품주문가능 , 등급에 따라 할인정책 , 할인정책은 vip는 1000원 할인 , 할인정책 변경가능인터페이스를 만들고 구현체는 갈아끼울수 있게\*\* 코드상 주문서비스 구현체에 할인적용을위해 private 인터페이스 =  new 할인구현체 코드가 들어가면할인 항목이 변경되면 주문서비스 구현체 코드를 변경해야하는 문제점발생.(남자주인공이 여자주인공 배역까지 결정하는 듯한 코드)--> 해결방안 (공연기획자가 등장해야한다)  APPCONFIG<img width="3666" height="2260" alt="Image" src="https://github.com/user-attachments/assets/bce94d8a-7bbe-4058-b794-c3ad657b694b" />\------------------------------------제어의역전 : appconfig를 만들었던것처럼 제어 흐름이 appconfig가 가지고있으므로 appconfig는 인터페이스의 다른 구현객체를 생성하고 실행할수 있기때문에orderserviceImpl는 그런 사실도 모른체 자신의 로직을 수행한다. 이처럼 제어 흐름을 직접 제어하는것이 아니라 외부에서 관리하는것을 제어의 역전이라한다.(IOC)프레임워크 : 내가 작성한 코드를 실행 제어권을 가지면 프레임워크(junit)라이브러리:  내가 작성한 코드를 직접 제어 를 한다면 라이브러리DI (의존관계주입)  : 정적인 클래스 의존관계 , 실행시점에 결정되는 동적인 객체(인스턴스)의존관계 존재    , 내부에서 의존관계주입이 아니라 외부에서 객체 주입정적인 - import코드보고 확인할수 있는것 ex) orderserviceIm은 MR과 discountpolicy에 의존  정적인것만으로 어떤 객체가 orderservimpl에 주입될지알수없음동적인 - 애플실행시 클라와 서버의 의존관계가 형성되는것을 의존관계주입 , 의존관계주입시 정적인 클래스 의존관계를 건들이지않고 동적만 바꿔서 실행가능IOC컨테이너 DI컨테이너 : APPCONFIG처럼 의존관계를 주입하는것스프링컨테이너는 @configuration을 설정 정보로 사용 , @Bean이 붙은것을 호출해 객체를 스프링 컨테이너에 저장\*\*스프링 컨테이너\*\*ApplicationContext 는 스프링 컨테이너 및 인터페이스이므로 applicationContext = new AnnotationConfigApplicationContext(Appconfig.class) 는 스프링컨테이너의 구현체\*\*스프링 빈조회\*\*ac.getBeanDefinitionNames = 컨테이너에 등록된 모든 빈 조회BeanDefinition beanDefinition = ac.getBeanDefinition(beanDefinitionName); = 모든 빈 하나하나의 정보,역활등에 대한정보beanDefinition.getRole() == BeanDefinition.ROLE\_APPLICATION  = 내가 등록한 빈이라면컨테이너에 등록된 빈조회 , 타입 중복조회 , 같은 타입일시 이름조회 , 특정 타입조회, 빈 이름으로조회 , 타입으로조회 , 구체타입으로조회 , 빈 이름으로 조회(에러)&#x20;\*\*스프링 빈 조회 (상속관계)부모 타입으로 조회하면 자식 타입도 함께 조회된다.